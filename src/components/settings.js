import React from 'react';

const divStyle = {
  height: 'calc(100% - 116px)',
  overflowY: 'scroll',
}

const Settings = () => (
  <div style={divStyle}>
    <h1>This is settings page</h1>
  </div>
);

export default Settings;
